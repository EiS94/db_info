# Deliberately unpinned: pytest-homeassistant-custom-component pins an
# *exact* matching homeassistant version as its own dependency (each
# release is extracted from one specific HA core release), so leaving it
# unpinned here means CI always tests against a genuinely current HA
# version, without ever needing manual bumps.
# See: https://github.com/MatthewFlamm/pytest-homeassistant-custom-component/releases
pytest-homeassistant-custom-component
pytest
pytest-asyncio